package javastudy260109;

public class Member {
/*	
	자바의 변수 종류
		1. 인스턴스 변수 : 클래스의 멤버 변수, 객체 생성 시 존재하는 변수. (당연히 객체 소멸 시 같이 소멸됨)
		2. 지역 변수 : 1) 블럭 {} 영역에서 선언되는 변수
					 2) for() 소괄호 안에 선언되는 변수
					 3) void sum(int a, int b) 메서드의 매개변수
		3. 클래스 변수 : 클래스의 멤버 변수, 프로그램 실행 시 클래스 정의를 메모리(Ram)에 로드하면서 생성
					  객체들이 공유하는 변수, 클래스 변수가 소멸하는 시점에 프로그램 종료
 */
	
// 클래스 변수
	static String dept;		//학과
	
//	인스턴스 변수
	String name;			//이름
	int age;				//나이
	String gender;			//성별
	int birthDate;
	
//	생성자 메서드(기본)	
	Member(){
	}
	
//	매개변수가 있는 생성자 메서드(선택)	
	Member(String name,int age,String gender){
		this.name = name;
		this.age = age;
		this.gender = gender;
	}
	
//	출력할 내용 확인
	public String toString() {
		return "이름 : "+name+", 나이 : "+age+", 성별 : "+gender+", 생년월일 : "+birthDate;
	}
	
	
	
}
